# For local dev: use PostgreSQL (e.g. Neon free tier) or SQLite (change schema provider to sqlite)
DATABASE_URL="postgresql://user:password@host:5432/dbname"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
EXCHANGE_RATE_API_KEY="your-free-api-key-from-exchangerate-api.com"
