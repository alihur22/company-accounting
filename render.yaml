# Deploy to Render: connect GitHub, add PostgreSQL database, set env vars:
# DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL (your app URL)
services:
  - type: web
    name: company-accounting
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NEXTAUTH_SECRET
        generateValue: true
