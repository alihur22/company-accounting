# Deploy to Render: connect GitHub, add PostgreSQL database, link it to this service.
# In Dashboard: add env var NEXTAUTH_URL = https://your-app.onrender.com
services:
  - type: web
    name: company-accounting
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NEXTAUTH_SECRET
        generateValue: true
